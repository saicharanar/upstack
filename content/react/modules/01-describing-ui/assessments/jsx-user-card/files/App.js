export default function UserCard() {
  // Build a profile card for Ada Lovelace using JSX.
  //
  // Requirements (each is one test):
  //   1. Wrap everything in a SINGLE parent element with className "user-card".
  //   2. Add an <img /> whose alt text is exactly "Ada Lovelace".
  //   3. Show the name "Ada Lovelace" inside an <h1>.
  //
  // Any image URL works for the src. Replace the line below with your JSX.
  return null;
}
