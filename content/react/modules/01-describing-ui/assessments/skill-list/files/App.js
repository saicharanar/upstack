const skills = ['JSX', 'Props', 'Conditional rendering', 'Lists'];

export default function SkillList() {
  // Turn the `skills` array into a <ul> with one <li> per skill.
  // Use skills.map(...) and give each <li> a unique key={skill}.
  return null;
}
