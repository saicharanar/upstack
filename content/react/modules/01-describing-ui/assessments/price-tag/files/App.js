const product = { name: 'Mechanical Keyboard', price: 49.5 };

export default function PriceTag() {
  // Use curly braces to read values from `product`:
  //   1. Show product.name inside an <h2>.
  //   2. Show the price inside a <span className="price"> formatted as "$49.50"
  //      (hint: {`$${product.price.toFixed(2)}`} ).
  return null;
}
