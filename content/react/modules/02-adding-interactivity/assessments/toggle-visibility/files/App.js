import { useState } from 'react';

export default function LightSwitch() {
  // A light switch that starts OFF.
  // A <button> flips it between on and off.
  // When it is on, show a <p> that reads "The light is on".
  return <button>Turn on</button>;
}
