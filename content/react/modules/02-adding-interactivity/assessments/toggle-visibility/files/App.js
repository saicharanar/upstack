import { useState } from 'react';

export default function LightSwitch() {
  // Build a light switch.
  // 1. Track whether the light is on in state, starting OFF (useState(false)).
  // 2. A <button> flips it between on and off.
  // 3. When it's on, render a <p> that says "The light is on".
  return <button>Turn on</button>;
}
