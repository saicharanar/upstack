---
id: adding-interactivity
title: Adding Interactivity
order: 2
summary: Events, state, and how React updates the screen — the heart of dynamic UI.
icon: ⚡
---

Components come alive when they respond to input and remember things. This module covers
event handlers, the `useState` Hook, how React actually updates the screen, and the rules
for updating state — including objects and arrays — correctly.
