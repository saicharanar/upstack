---
id: real-world-projects
title: Real-World Projects
order: 7
summary: Move from isolated concepts to features you'd actually ship — a methodology for turning a design into components, plus two projects that fetch data and validate forms with all the real states handled.
icon: 🚀
---

The earlier modules taught concepts one at a time. Real features combine many of them at once —
and add the parts a demo skips: loading and error states, empty states, validation, and the
messy edge cases. This module starts with a repeatable way to *think* about building UI, then
walks through two projects that put it into practice.
